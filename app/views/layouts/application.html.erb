<!DOCTYPE html>
<html>
  <head>
    <title>Shikitabi</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://fonts.googleapis.com/css?family=Abril+Fatface|Noto+Serif+SC|Patua+One" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script type="text/javascript" src="/assets/infinitescroll.min.js"></script>


    <!-- 以下Googlemap読み込みの為の記述 -->
    <!-- ENV['']内にAPIアドレスを記入する -->
    <script src="//maps.google.com/maps/api/js?key=<%= ENV['AIzaSyAiWcd-kJAomWmCzkzeqhLvpaBh313j_F0'] %>"></script>
    <script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"></script>
  </head>

  <body>
    <%= yield %>
    <!-- 以下Flashメッセージ -->
    <% flash.each do |type, msg| %>
      <% type = 'info' if type == 'notice' %>
      <% type = 'error' if type == 'alert' %>
      <script>
        toastr['<%= type %>']('<%= msg %>');
      </script>
    <% end %>
  </body>
</html>
